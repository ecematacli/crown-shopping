import React, { Fragment } from 'react';
import LoginForm from './loginForm/LoginForm';

const LoginPage = () => {
  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  );
};

export default LoginPage;
